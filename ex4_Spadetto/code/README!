Just run

python3 script.py

it will generate the "out" directory, then at the end inside you will find
many subdir "opt*" where * can be:

N = no optimizer flag
1= -O1
2= -O2
3= -O3

inside there will be tha datas, and a plot: logscale for the time, linear for N(matrices dimension), this plot is just for debug.

Another plot will be generated in the folder where script is runned,
consider it as the plot requested for this assignment.
